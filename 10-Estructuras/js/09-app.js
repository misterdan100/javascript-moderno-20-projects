const autenticado = false;
const puedePagar = true;

autenticado ? puedePagar ? 'Si esta autenticado y puede pagar' : 'Si esta autenticado, no puede pagar' : 'No puede pagar'

if(autenticado) {
    if(puedePagar) {
        'Si esta autenticado y puede pagar'
    }
}