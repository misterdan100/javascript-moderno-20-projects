const nombre = "Monitor de 20 pulgadas";
const precio = 300;
const disponible = true;

// Un objeto es una estructura de datos que agrupa valores o funciones

// objeto literal
const producto = {
    nombre: "Monitor de 20 pulgadas",
    precio: 300,
    disponible: true
}

console.log(producto);